server:
  port: 9090

spring:
  security:
    user:
      name: aral
      password: root
  rabbitmq:
    host: localhost
    port: 5672
    username: aral
    password: root
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:9999/TemporaryAuthDB
    username: postgres
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true