server:
  port: 9094

spring:
  rabbitmq:
    host: localhost
    port: 5672
    username: aral
    password: root
  mail:
    host: smtp.gmail.com
    port: 587
    username: ${aral_gmail}
    password: ${aral_gmail_password}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true